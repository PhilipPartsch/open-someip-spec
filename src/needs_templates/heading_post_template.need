{{ h | default(6) }}
{% set symbols = {
    0: '#',
    1: '*',
    2: '=',
    3: '-',
    4: '^',
    5: '"',
    6: '_'
} %}

{% if h == 0 %}
.. rst-class:: break_before
{% endif %}

{{ title }}
{{ symbols.get(h, '_') * title|length }}
